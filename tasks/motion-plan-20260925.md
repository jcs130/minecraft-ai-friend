# Motion plan and Jev step correction

## Observed gap

The tested `base_navigate` program already walks through multiple local survey and goto segments while Qwen is busy. The sixth live 600-second window confirmed four long goto segments across two programs, but a finished or replanning program leaves the body idle until another slow decision. Jev currently chooses a skill or interrupts a native task; it does not choose among the freshly surveyed next navigation segments.

## Implementation

1. Add a bounded, typed 2–6 waypoint `navigate_plan` tool. It resolves an explicitly tested `base_motion_plan` skill and submits one durable skill job, leaving the existing action quota and body lease unchanged.
2. The new program validates each waypoint inside the work area, advances only after an exact goto receipt and observed arrival, and uses the existing 16/8/4 survey fallback for each leg.
3. At each fresh survey with supported candidates, ask Jev to choose a specific next goto or escalate. The controller binds the selected exact coordinates into program memory before dispatch. A stale body, low confidence, unavailable policy, unsafe native preflight, unknown outcome or failed leg stops the plan and returns to slow planning.
4. Verify isolated skill fixtures, tool admission, Jev alternatives, negative and crash cases. Do a full isolated suite before any live install/deployment, then observe a natural live window.

This first plan keeps movement going across several LLM specified waypoints. Other queued game actions remain available through the existing six request mailbox; the native receipts still decide whether they succeeded.

## Deployment and measured behavior

- `base_motion_plan` version `38093985db1e47571412e1bdca45050ca49e5f5b29248cd317372a39e2ff50bf` passed its five real engine fixtures and was promoted while both relevant services were stopped and the controller was drained. The native driver exposes `navigate_plan`. The user's 27 enabled native tools were preserved, including general `execute_shell_command`; an installed ToolGuard permission check returned `ALLOW` for `pwd`.
- The isolated source suite before the latest lock fix passed 977 tests with three explicit legacy skips and 38 practice tests, with hashes matching its archived source tar. It does not prove a natural model will select the new route tool.
- The first post-deployment 600-second natural sample (`runtime/continuous-navigation-live-1790272929/summary.json`) had no movement because the then-active `home_llm/qwen3.8-27b-instant` requests timed out. The prior configured `deepseek/deepseek-v4-flash` was restored by changing only `active_model`; current tools and shell settings were preserved.
- With DeepSeek restored, the next 599-sample natural window (`runtime/continuous-navigation-live-1790273540/summary.json`) observed one confirmed goto of about eight blocks, four slow tasks, and two in-game lines with completed speech receipts. Its longest idle stretches were 207 and 191 seconds while the slow task ran. The model selected legacy `navigate`, so this sample does **not** demonstrate live multi-waypoint or Jev correction. One `navigate` call was rejected as `action_busy` before admission, then the model ended that turn. The original role instructions still recommended only `navigate`; the follow-up source change now describes `navigate_plan` and its bounded waypoint use.
- `SkillTools._write` previously used an immediate mutex attempt, unlike direct body actions, causing a harmless concurrent holder to discard the planned route. The follow-up fix waits the same bounded admission interval as direct actions and returns an explicit before-lock, no-dispatch receipt permitting one exact-request retry. The native turn reference now distinguishes that receipt from unknown or in-flight work. Concurrent admission tests cover both short and sustained contention.
- The first window after the prompt/admission update (`runtime/continuous-navigation-live-1790274815/summary.json`) observed a **real five-waypoint `navigate_plan`** and exact queue acceptance of `base_motion_plan`. Its first Jev selection stopped with `policy_escalated`: three supported same-height stances within about one block received 0.50, 0.41 and 0.03 probability, while `replan` received 0.06. The 600 samples contained 15.26 blocks of path and 13.45 blocks of net displacement, four text+audio completions and five slow tasks, but the motion plan executed no goto before handing back; later travel came from another action. Longest idle stretch was 152 seconds. This is failed continuous-plan evidence, not a pass.
- The follow-up source update offers Jev one representative per cluster of nearly coincident, same-height supported stances; materially different XZ and heights remain separate, as does the explicit stop option. The actual body identity, fresh survey, work area, native goto preflight and receipt checks remain in force. This avoids treating three nearly identical landing coordinates as three distinct decisions.
- One read-only replay of the recorded live body state with the clustered choice set returned Jev `path_0`, confidence `0.87`, probabilities `path_0=0.94` and `replan=0.06` (896.92 ms). The original live choice had confidence `0.32`. This is evidence for the candidate-duplication diagnosis, not a world action or natural-run result.
- A later natural two-waypoint plan on the previous skill version did dispatch one Jev-selected goto: the exact native task `t93` completed and the body moved from about `(-534.57, 875.50)` to `(-547.48, 884.50)`, approximately 15.7 horizontal blocks. The next fresh survey offered genuinely different terrain heights and directions, and Jev returned confidence `0.20`; the program correctly replanned. This proves one motion-plan segment, not full route completion.
- Final clustered source QA in `runtime/motion-candidate-qa-vertical-20260925/`: 979 embodied tests passed with three explicit legacy skips, 38 practice tests passed, 130 embodied and 16 practice source hashes still match the host; network disabled, no production mounts, model calls, world actions or production mutations. Twelve focused motion tests also passed.
- The exact old motion version `38093985...` was upgraded offline under drain and both services stopped. Preview matched the expected new source SHA `105853aa...`; six real-engine fixtures passed and `base_motion_plan` active version became `1f1a7605dfe4b9beb46d71c05bd029fe125f6fd6f0109fb58dd99d07b6ea1e4f`. The old version and a private backup remain in `runtime/motion-skill-upgrade-cluster-20260925T0251/`.
- The first natural 600-sample window on that clustered skill (`runtime/continuous-navigation-live-1790276285/summary.json`) accepted a four-waypoint motion plan, but Jev's pending decision was discarded after 8.52 seconds with `policy_premise_changed`. The next tick reused the now-stale `navigation_sense` observation and immediately stopped with `navigation_survey_unusable`; no goto was dispatched by this motion plan. The body still covered about 85.3 horizontal blocks of sampled path, with three public text messages and completed audio receipts, through other navigation and actions. Thus the window shows movement and narration but does not demonstrate a completed multi-waypoint route.
- A new controller regression test reproduced the expired-choice failure in the isolated pinned container (only one survey), then passed after the controller cleared the stale survey stage and observation on a discarded motion-plan policy result (two fresh surveys and zero body actions before a new decision). The existing maximum of two such retries remains. Full source QA in `runtime/continuous-navigation-source-qa-d441171e49/summary.json` passed: 980 embodied tests, three explicit legacy skips, 38 practice tests, 130 and 16 matching source hashes, no network or production mounts. Live verification of this follow-up is pending.
- After the completed drain, managed qwenpaw/survivor restart and resume, both services were healthy, the controller source contained the recovery fix, the role prompt still matched its source, and all 27 user-enabled native tools remained enabled with `execute_shell_command` allowed. The observer stayed online and following at zero reported distance.
- The next 600-sample natural window (`runtime/continuous-navigation-live-1790277540/summary.json`) produced 56.88 blocks of sampled horizontal path and 45.88 blocks net displacement, five completed goto receipts, and four distinct public text messages with completed speech receipts. The longest stationary interval was 103.51 seconds while the slow task ran. It selected no `base_motion_plan`, so it neither proves nor refutes the controller fix in production. Model counters increased by 65 underlying requests, seven decisions, 2,770,746 cumulative prompt tokens, and 100,969 completion tokens during the window; these are summed across requests, not the size of one context. The lack of a queued route during long slow turns remains a material streaming limitation.

The live multi-waypoint and Jev behavior still need a natural execution trace after the follow-up controller fix; do not infer them from isolated fixtures or service health.
