# 游戏 QwenPaw 2.2.1 升级

核查日期：2026-09-14。仅升级 D 盘项目的游戏 Docker QwenPaw（18089）；原10角色、16项定时任务及生活会话保留。Minecraft、Numen 身体和宿主 QwenPaw 不随包升级重启。原生能力与可复用候选见 [能力与市场研究](QWENPAW-CAPABILITY-REUSE.md)。部署验收结果在本文件末尾记录。

## 发行与构建

官方 GitHub latest 与 PyPI 均为稳定版 **2.2.1**，发行时间为2026-09-11 04:19:23 UTC。依赖 **AgentScope 2.0.7.post1、ReMe 0.4.1.11**。[官方发行](https://github.com/agentscope-ai/QwenPaw/releases/tag/v2.2.1)、[PyPI](https://pypi.org/project/qwenpaw/2.2.1/)。

先在无生产状态的构建中实际执行 `qwenpaw update --yes` 并校验三项版本及依赖，再用已锁基础镜像构建可恢复的正式发行：

```powershell
.\run-python.bat tools\build_qwenpaw_recovery.py --build --target game --tag-suffix memory1
```

正式镜像 `qiandengji-qwenpaw-game:2.2.1-memory1`，image ID `sha256:41ddc296a4e8f281a18651c0a06dbd7c1b90b29dece4a40f6fa9b97812c408c4`。原游戏镜像 `2.2.0-autonomy16` 与原身体镜像 `2.2.0-autonomy18` 保留，身体只连接新的游戏 QwenPaw API，不再运行另一份模型 Agent。

源码清单与实际构建回执保存在本机 `runtime/qwenpaw-recovery-20260914T101042Z-f075a517/` 和 `server/runtime-images/game-recovery.json`。镜像基线与挂载源码分开记录，不把后续文档编辑伪称为旧镜像构建输入。

## 兼容处理

- 新版原生 ReMe 已修复状态统计；2.2.1直接使用上游实现，旧版统计补丁只保留作回退兼容。
- 保留精确会话请求、游戏动作真实证据、正常回合收尾、单并发和工程长任务适配。天神 GLM-5.3 没有新增总时限；升级不改变角色即时模型选择。
- MakeSkill 2.0 改为四个官方脚本。更新10角色与公共技能池的完整官方包，并同步原生工具权限和用法；不再调用已移除的 `materialize_skill`。精确原包扫描例外及调用边界见能力文档。
- 继续原 ReMe、每5个外部回合提炼、每小时 Dream、原10分钟复盘信号。保持当前上下文策略；Scroll、外部记忆服务及财经 Auto Fin 不混入这次迁移。

版本、依赖和受影响上游函数都按实际源码校验。未知版本需要重新审阅，不能只改版本字符串跳过验证。原版本与新版都通过了隔离组合检查，回退不依赖卸载新版后重新猜测依赖。

## 状态迁移与维护

1. 保存原16完整 Cron 配置与10角色模型配置，暂停新任务，排空桐人生活回合；NPC维护闸门仅拒绝新的模型提交，原任务仍可查询。
2. 等原任务、运营锁及记忆整理自然结束，还须核对 NPC 的 `qwen-tasks/active-roles` 所指账本已保存原任务终态，不能只看 Qwen 活跃数。18点结衣和桐人的 Dream 分别在18:04:56、18:10:51成功，18:14最终复核任务与记忆均空闲。
3. NPC以 SIGINT退出，Qwen正常退出，两者 exit code均为0。停止后备份5,910个文件、334,873,985字节，并逐文件核对；此备份不冒充仍运行的 Minecraft 世界或身体快照。
4. 先在断网、无模型、独立状态副本运行最小迁移，再应用同一工具到已停止的生产状态。只更新 global scanner、角色原生工具与对应策略、官方技能包、技能演化说明和三段原生用法提示；不重建人格、会话、记忆或 Cron。
5. 先启动带维护闸门的 NPC 以提供 MCP，再启动 Qwen。确认版本、全部角色、技能及原生健康检查后，恢复原 Cron 和生活循环。

维护记录与不可改写备份位于本机 `runtime/qwenpaw-upgrade-20260914/maintenance/`。`tools/control_qwen_admission.py` 在现有 NPC 容器内运行，必须用同一维护编号恢复；它不是 Agent 工具或新增常驻进程。NPC进程需要重载才会看到新代码。

NPC主入口处理 `KeyboardInterrupt`，因此 Compose 明确使用 `stop_signal: SIGINT`，避免以后默认 SIGTERM 等到超时才强制结束。

不要重跑 `init_qwenpaw.py`、旧双角色 `upgrade_qwenpaw_runtime.py` 或完整初始化同步。它们是早期迁移，不能覆盖当前团队。若需要回退，应先重新排空服务，保留升级后新增历史，再使用本次备份及原镜像做明确恢复；不能直接把旧状态盖到新会话上。

## 验证范围

隔离检查已覆盖新旧上游适配、原生记忆状态、真实 MultiAgentManager 就绪行为、长任务、生活会话、权限及渐进披露。官方 MakeSkill 在扫描器 block 模式下真实完成规划、草拟、校验、本地发布，生成技能可被原生 SkillService 发现；没有上传市场或请求模型。

市场内容本轮仅研究、未安装第三方框架。已提供游戏设计和任务策划候选及原作者固定提交、许可证；不会把“包存在”称作角色已经掌握了新玩法。

## 正式部署结果

18:31:28 新游戏 QwenPaw 启动，实际 image ID与上述正式构建一致。18:33 完整原生健康检查通过：版本2.2.1、10角色、96项启用技能绑定（其中官方30项）、101次原生只读请求、学习MCP与身体/队伍权限全部通过；基础原生工具由7项变为6项，是移除旧 `materialize_skill` 后的正常变化，技能创建通过官方脚本继续可用。全部13项默认 Compose 服务运行，带健康探针者均正常；原Minecraft和survivor进程启动时间保持不变。

正式最小迁移与候选使用相同脚本 SHA256 `23e9f83de9f0d6e76407e691b17f4aac0320c285c86d17b9684b80e8a439b7dd`。候选首次只变更130个预期文件，4637个原工作文件字节不变；再次完整执行4767个工作文件字节全等。正式迁移回执为 `server/agents/upgrade-221-backups/221-20260914T102944896992Z/result.json`。官方技能的当前来源更新为2.2.1，用户启用、渠道、配置及未知自定义字段保持；原生技能清单版本时间戳正常刷新。

18:33:49起释放维护闸门，原16项Cron及桐人自主控制恢复；即时名称、模型、fallback与thinking字段核对相同。18:32新版10个角色的记忆状态接口已全部成功返回，没有新建第二套记忆整理循环；下一次Dream按原19:00班次运行，不把升级前18点成功记录当成新版Dream已经执行。

本次结衣 `task-256878865747` 的旧进程会话已结束，但NPC在停机前没有收取终态。原生 `_bg_tasks` 是进程内缓存；新版真实查询404、角色idle/0任务、NPC已停止三项确认后，仅写入独立 `released_without_result` 维护凭证以解除新输入阻塞。原请求、活动指针、session、预算字节均保留，原结果仍未核实，没有重发旧输入或标记成功。证据位于维护目录 `yui-lost-receipt/result.json`；今后停机要先回收所有已知原任务终态。

18:34–18:39的新版实机窗口：桐人原生活会话 `task-3cb90737680e` 自主吃掉1个腐肉、饥饿8→12，完成4段导航；一次严格到达失败后自行换路成功，并写入学习记录。18:38:18控制器确认原生任务完成，18:38:33自动进入下一轮 `task-96294f6eb6f9`。结衣原会话两轮已完成、第三轮继续，两角色各只有1个活动任务。观察只读，无人为模型提交或代做游戏动作；这些证明升级后循环恢复，不代表建造、装备、耕作或长期自我进化全部验收完成。

全项目 `health_mon.py` 已运行；运行服务和新版Qwen核心检查通过，但旧architecture、技能栏等历史行为记录仍有来源不匹配项。原记录保留，未修改旧哈希制造通过。详细当前结果在本机 `runtime/qwenpaw-upgrade-20260914/final-health.log`，与本次升级的健康和生存回执分开判断。

巡检另修复宿主Qwen2.2.0与游戏2.2.1并存时的版本比较：三类文字工作角色仍按完整原生工作区规则验证，跨版本时在现有游戏容器中读取其实际依赖、运行标记及源码进行验证，不放宽工具或安全断言。修复后15项路由、6项检查全部通过，宿主无需升级。旧 `agent_learning` 审计生产器还指向已退役18090，本轮不以改旧来源证明代替重新验收；当前学习能力的证明采用新版101次原生接口核查、官方四脚本实测及实际生活复盘。
