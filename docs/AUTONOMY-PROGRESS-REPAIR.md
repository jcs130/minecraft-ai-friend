# 自主生活、学习与运营修复（2026-09-14）

## 本轮解决的问题

桐人在旧任务里反复等待作物、尝试用放泥土腾空背包；一次保存进度后又查空感知，最终触发原生 Doom loop。调查还发现实际持续生活入口没有带入已有资源/装备摘要，长期任务被先前一次临时等待指令覆盖，ReMe 自动学习看不到工具回执。

1. 现有 `life_context` 带入 `adventure`、主包空槽、长期使命和当前能力资料。通过原生 API 同步 13 个已有技能绑定、追加桐人当前操作说明，恢复原自主长期任务；没有清除历史、另建人物或规定固定的农耕路线。
2. `remember` 新增可选 `finish_turn` 和模型自己写的 `summary`。只有当前成功保存回执才进入原生文本流和会话持久化；中途保存不退出，错误/未知回执不退出，后续待执行工具、原生停止处理仍保留。
3. 首次上线实测发现 Qwen 的内部 `AgentState.session_id` 是随机 ID，外部 `life-…` 在官方 `_request_context`。最终 finish v3 使用后者与角色、用户、频道精确绑定，保留原内部会话 ID。
4. 原生压缩可能移除本轮用户输入，让模型把 `t468` 或自造记忆编号当 `turn_id`。请求适配 v1 将本次调度已经提交的原始编号放入原生 request context；每次原生模型输入准备后附上该请求引用，含原生溢出恢复分支。不查询/补发新租约，不替换工具参数，不续期、不重试、不改历史记录。
5. 结衣原生活输入补齐当前工具/工作资料和有时效的救援观察。原生 TLM 工作设置仍由模型按现场自主选择；配置成功与实际采收分开核对。[细节](YUI-WORK-AND-RESCUE-CONTEXT.md)。
6. ReMe 原生 Auto-Memory、Dream 扩展只作用于原桐人/结衣。自动记忆附上按真实 call ID、身体、动作回执配对的有限证据，旧错误以原生 ETag 追加纠正并保留来源。[细节](LIFE-MEMORY-EVIDENCE.md)。
7. 天神工程 A2A 和原 Cron 共用单并发锁，精确工程派工取消总截止；司灯日报可在完整核验工程占位后并行。账本按实际完成时间保留最近终态，避免刚完成的任务被立即裁掉。[细节](ENGINEERING-EXECUTION-REPAIR.md)。

没有新增宿主循环。模型、16 个原生 Cron、两个人物 UUID/背包/原生活 session、Minecraft 存档和宿主 QwenPaw 均保留。天神 GLM-5.3 的供应商错误和原生流空闲超时仍按真实结果报告，不把取消总截止说成永不失败。

## 验证与部署

在锁版本镜像中禁网运行 277 次生活/工具/记忆/伙伴回归，全部通过，包含真实 Qwen/AgentScope 回复流、内部随机 session、官方请求构建器和压缩后模型输入。另有 81 次工程派工/锁/账本/调度回归及 11 次 Windows bind mount 锁测试通过。测试均不调用供应商、不操作生产身体；其中复用夹具类导致部分执行重复，不代表新增 369 个独立场景。

15:53 首次部署 autonomy15，15:55:45 恢复原班次；两个新原生活回合证明资源/长期目标信息实际进入原生 dialog。桐人换高度后导航成功、真实吃鱼并返回营地，第二轮成功调用 `remember(finish_turn=true)`。该时点 v2 的会话身份判断未命中，后面仍有一次模型答复，因此不能用这次来验收 v3 的直接结束行为。

司灯 15:57:35–15:58:26 原生补跑真实完成，生成 `world-daily-2026-09-14` 并请求次日公会内容；同一账本记录从 pending 到 completed 后仍保留，已获线上验证。女神 16:01 的自然巡查 138.058 秒成功。天神 16:05 的原生班次实际持有正确工程锁、周期和占位，无总截止。该窗口没有新 A2A，不能称生产长 A2A 或日报与工程并行分支已线上覆盖。

维护记录、原生 API 前后字节、备份、逐任务和工具证据存于本地 `runtime/autonomy-repair-20260914/`。首次停止的三个 Agent 服务备份 6,556 文件、242,419,432 字节逐项 SHA 校验；主服未停止。后续最终部署与新窗口结果追加于下文，不覆盖首次失败或旧报告。

## 仍需按实际运行确认

结衣新资料已进入真实会话，但首轮选择等待，尚无她主动农耕或装备分工的新产出。桐人新增丢弃接口和目标摘要可见，不等于本窗已整理背包、打造装备或建好房屋。

ReMe 自然整理已生成不可变证据，结衣新无动作轮没有被写成进食/采收；但 Dream 仍会将旧日记概括成未经验证的永久规则，例如把某次 `lease_invalid` 直接解释为租约到期、把旧农田观测推广成普遍玩法。锁版本原生三条学习系统提示均验证包含证据规范，目前没有发现钩子遗漏。模型知识质量仍需检查，不能把证据接线成功称为学习结果全部正确。

新实际健康检查与旧来源证明分开。旧全项目源码哈希漂移不通过改写报告凑绿；没有实际新动作/新通信的分支不借用旧窗口当作本次验收。

## 最终增量上线

16:05 原天神任务在 16:11:33 自然成功，用时 393.085 秒。期间仍在读取候选源码、查测试和写记忆；即使 agent-status 一度为 idle，原 Cron/锁尚在工作，因此维护等待真实终态。最终原账本 completed 保留、锁已释放，所有原生占位清空后才停止三个 Agent 服务。

第二次备份 6,639 文件、240,798,921 字节逐项校验，位置 `runtime/autonomy-repair-20260914/rollout16/backup-20260914T081253Z/`。16:13 部署：

- 游戏 Qwen：`qiandengji-qwenpaw-game:2.2.0-autonomy16`，镜像 `sha256:9d2d84e01849a94936daf3ab6fd4b1dbaf8a1a658c0f82e3ce998c29b1665afa`。
- 生存服务：`qiandengji-survivor:2.2.0-autonomy16`，镜像 `sha256:08eba4aded6eb6828a101ce59061d15ffced5306d580176bddd6d64807f610ef`。
- 构建记录：`runtime/qwenpaw-recovery-20260914T080921Z-b0f317b1/build-result.json`。

原生 Qwen 健康检查 101 次只读请求、53.558 秒通过，实际进程确认 finish3、request1、memory1、guard5、10 个角色和 96 个技能绑定。独立审查又以真实官方提交/构建器、OpenAI 消息格式化、原生 provider 溢出恢复、两个钩子组合完整回复验证 3 项通过，输入引用不进入历史，原工具配对和参数不变。

16 个原 Cron/模型配置与生存服务在 **16:15:44 +08** 恢复，最终新窗口基线为 `rollout16/life-before.json`。重启前后桐人官方自动记忆计数均为 pending 3/5、seen 157，原 session 字节 SHA 未变；未为测试主动触发总结。最终生活回合与自动记忆实测结果继续追加。

最终首轮 `task-159278514891` 于 16:15:50 自然提交、16:17:09 正常完成。`remember(finish_turn=true)` 成功结果后约 48ms 输出文本，与该次调用参数、成功回执、原生任务最终文本的 summary 逐字一致，后无新增 thinking/tool 块；外部会话与内部随机 ID 均保留。这次才是 v3 直接结束的线上证据。5 次真实导航为 2 次到达、3 次失败，全部使用当次正确 turn，无 lease_invalid/Doom；16:17:51 下一原会话轮自然启动。

16:18 最终 13 服务全运行、所有配置的健康检查正常。独立旧 `world_team_health.py` 仍有七项失败，不能笼统归为源码哈希：启用列表与含退役/休眠署名的 14 人历史名册比较、访问已退役 18090、四个已禁用旧角色检查，以及旧 `team-python` 验收计划无法匹配当前 `team-guild-admin-python` 导致 StopIteration。实际现役 18089 的 10 人/96 技能检查已经通过；旧审计与历史工程证据保持原样，本轮未将这些断言放宽凑绿。

第二轮 `task-1a8a88663b94` 16:21:49 正常结束，4 次导航、2 次土豆进食成功，主包空槽由吃完一叠土豆增加，不能记成 drop。该轮 6 次 remember 都是正确 turn_id 但漏 summary，被原参数校验拒绝；额外一次身体调用返回 lease_invalid，和这些参数错误分开保留。由此补充明确错误回执：`finish_turn=true` 必须同时给非空 1–600 字 summary，参数失败未写记忆/未结束，不是租约失败；不替模型编造摘要或补编号。新增回归与既有收尾/技能工具共 57 次执行通过。两轮原 life smoke 已真实通过，不能因通过而隐去上述交互问题。

16:19 原生压缩触发提前 flush，并非人工补足 5 轮：将 3 个 aut15 与 1 个 aut16 的待整理回合交给原 Auto-Memory。64 个调用精确配对，16 个动作观察为 10 completed、4 failed、1 rejected、1 in_flight；只有 10 项计作成功，其中 4 个回执来自 aut16 新窗口。原生学习真实完成 3 次写入，并自行纠正一次漏 path 的编辑。新日记正确记录吃鱼/胡萝卜体征变化、导航成功与失败、未验证的放置失败，未把当前无动作补成成功。独立原 dialog 配对核验 5/5，原学习输入包含当前工具和纠错来源；仍有旧目标/传送推断、未自发采用 drop、未给新证据直接链接，不能称所有知识推理都正确。证据在 `rollout16/memory-verification/report.md`。

16:27 仅生存服务部署 `qiandengji-survivor:2.2.0-autonomy17`，镜像 `sha256:3ae13724de7c18be4fe9a09cc97bb4213a536ab9d50e5847313bffed6a09ff15`，补齐上述参数错误提示。Qwen 保留 autonomy16 原进程，NPC/主服未重启，16 项原 Cron 未停。桐人当轮自然收尾后才单独停止 survivor，备份 1,156 文件、14,043,540 字节逐项核验，位于 `rollout17-082712/backup.json`；新健康通过，16:27:52 恢复。此前 aut16 两轮证据保留，最后增量另以 `rollout17-082712/life-before.json` 开新观察窗口。

## 回执并发故障与最终维护

aut17 在 16:30 被原生复盘任务自然唤起，16:32:08 的一个 goto 在服务端实际出现吻合的到达，但返回链路产生 GatewayError，原生回合随后完成、身体控制器按未知动作保护暂停。原始错误正文未保留，不能确定这一次具体命中了哪个异常分支。继续审查实际部署的 NeoForge 字节码，确认不同 RCON 客户端共用同一返回缓冲区，完整命令事务没有串行保护；Numen 也存在任务已开始、JSON ACK 尚未完成的窗口。[根因、修复及隔离实测](RCON-COMMAND-ISOLATION.md)。

16:58 通过 `tools/reconcile_kirito_goto_observed.py` 人工核验原身体、生活会话、Qwen 终态、原未知字节与原生 t554 的到达/事件后，归档恢复决定并仅关闭这个旧租约及活动阻断。原动作仍为 unknown，`effectAttributionVerified=false`、`originalAckRecovered=false`，没有补造 ACK、重发动作或改旧历史。11 项恢复测试及提交中断恢复检查通过，证据在原状态的 `reconciled-actions/114d50e64e3f460589e94ec5014cbcdc/`。

survivor 增量还会私下保留后续未知动作的实际原生回执、摘要哈希和安全错误码；模型仅看到诊断记录是否保存。种植缺耕地时，将本次预检已读取的作物格/支撑格及请求坐标返回给模型，明确 plant 使用作物空气格、till 使用地面格，不再只给模糊错误；不多读一层、不替模型改坐标或重试。包含这些路径的 152 次生存回归通过。

旧团队巡检的退役角色误报已经修正：10 个现役角色独立核验，4 个历史/休眠身份单独保留，已无现役用途的 18090 不再请求。工程基线由正式 runner 真实执行新任务 `world-team-baseline-20260914-current-plan-01`，16:58:05 完成 145 次测试、4 项批准文件校验、容器清理；候选 HEAD 和 853 文件字节保持原样。健康显式迁移至新基线，指定旧基线仍返回旧计划不匹配，未覆盖历史。16 项相关回归通过，记录在 `engineering-baseline-review-20260914/report.md`；这不等于天神的候选业务改动已部署。

16:51 暂停原 16 Cron 接新轮次，16:57 所有原生任务/锁自然释放。16:59 主服正常保存全部维度并停止。备份 `rollout18/backup-20260914T090117Z/` 共 36,193 文件、3,054,644,179 字节逐项校验，包含完整 shadow 存档、原 Agent 工作区/状态、工程候选与历史。

17:04:36 部署服务端 botgate `6ca6c1ed262417187a0447e727c3a753a8b82d4d50e30442b64a55ff2b15022a`，仅两 class 变化；客户端不变。原 build-record 原字节归档到 `world/botgate-src/history/`，新来源记录和两个实际部署锁同步。survivor 为 `2.2.0-autonomy18`，镜像 `sha256:70e2ee830472818fb017c3486a21eacaa42dd73d2bc9269e579a320cbfe9f3ef`；Qwen 仍为 autonomy16，因本次完整维护重启，宿主 8088 不变。正式协议验收及恢复后的新生活窗口继续在下文记录，不能以先前 aut16 的两轮证明当前结果。
