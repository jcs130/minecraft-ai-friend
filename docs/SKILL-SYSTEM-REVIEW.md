# 技能罗盘、文字接口与运维复盘

2026-09-20 已部署到原实服。此次处理用户反馈的菜单混乱、技能不可用、铁魔法缺少指引，以及运维 Agent 如何继续改进技能体系。

## 修复与当前行为

- 罗盘保持既有六行容器，默认“我的技能”：已学/天赋女神秘术和实际装备原生法术。未学内容移到学习图鉴；分类、指南、传送阵和快捷栏沿用原入口。中文说明自动折行。
- 原生主题别名不再被旧 learned/等级规则错误锁住；装备、魔力、冷却、正在施法等原因可见。不可用项不能派发，点击保持菜单，恢复后刷新。
- 实服原目录只有 8 项，仓库已有 35 项。现在统一部署原有目录：9 项女神秘术、26 个主题别名（映射 21 个原生法术），37 项历史档案保留。别名仍需真实装备，不复活旧动漫效果，不授予免费法术或重写进度。
- `help irons`、`menu guide` 提供卷轴→铭文台→书→装备的入门；`help <ID>` 按需返回条件、引擎、参数及下一步。`skills --json` 返回简短索引，执行结果和后续感知才证明效果。旧女神魔力与原生魔力分开描述；映射项的旧成本/等级不再冒充有效规则。
- 旧平衡覆盖层拒绝铁魔法映射项，防止成功改了无效数值；非有限数值也被拒绝。此次没有擅自调整强度。

使用方式见 [技能与 CLI](SKILLS-UNIFIED-CLI.md)。真人入口为技能罗盘/F6，右上角指南；Agent 沿原 `game_skills`、`game_cast`、`game_skill_receipt` 使用，没有新建身体控制循环。

## 运维与自我改进

通过 QwenPaw 官方技能接口更新 10 个现役角色已启用的 `qd-world-team` 和 `qd-minecraft-guide`，原生扫描、enable 与逐文件读回通过。归档体验官的源码指引同步修正，但没有重新启用休眠角色。

[技能平衡与性能指引](../world/ops/skills/qd-world-team/references/skill-balance-performance.md) 明确 L1 实际操作回执、L2 条件化经验、L3 同场景基线/单因素候选/隔离验证/回退的要求。用户要求已通过原 TeamStore 登记工单 `case-ad416b71edb9e6d6d43a`，注明由维护者代用户录入，交现役工程师；没有伪称 Agent 自主发现。

现役固定工程计划仍只覆盖团队与具身路径，缺技能 Java/TS 和 Iron 配置覆盖。工单要求先提出固定验收计划和候选环境升级方案，保留工程师此前未部署的候选与历史记录。原禁用工程 Cron 保持禁用。因此本轮证明界面/接口修复和指引接入，没有证明自主数值平衡、性能收益或完整 RSI 闭环。

## 验证和部署记录

- Java 285 项断言；Node 97 项相关测试通过。健康证据失配、缺项、缺报告的负向回归通过。
- 独立 72 模组测试服 7 项真实协议检查通过：默认目录、指南、缺装备禁用、实际装备识别、点击后隐身效果与魔力/冷却变化、冷却禁用、防虚拟物品转移。两次早期失败回执保留：第一次为测试误认目录顺序，第二次未在观察窗口看到效果，不推断原因；第三次完整通过。未做真人手柄或第一人称画面验收。
- 正式备份 `2026-09-20T153047.0013444Z-eb1c2efb` 完成且 save-on 确认。自然 drain 后停止原 world/npc/survivor/mc，按 30 文件精确清单备份前像与部署，随后原服务恢复。无角色档案、背包、经验、技能账本或私人地点替换。
- botgate `8f669d9fe4d70a24c6967db28b9867963469603c44685f13371685d7e5c0a240`；目录 `a44beebfb78985a17d025b768bcf38622e91d0e33134068569b1b49bd5c17cbf`。旧构建记录保存在 `world/botgate-src/history/build-6ca6c1ed2624.json`。主城保护桥与 Numen 未替换，原保护 4 项通过。
- 原 10 角色 profile 与 16 Cron 精确恢复，桐人沿原控制器重新上线。实服 `help irons`、`help tp`、`skills`、`spells legacy` 四项只读回执通过，没有在桐人身体上测试施法。
- 新 `skill_system` 探针已进入原健康 manifest 和 panel smoke，验证当前部署字节、目录镜像、隔离交互证据及实服读回。证据 `reports/skill-system-review-smoke.json`；私有维护资料 `runtime/skill-system-review-20260920/`。历史全局红项与旧验收报告不覆盖、不改哈希凑绿。
- 面板全量检查并非全绿：旧技能栏等报告未覆盖新构建，保留失配。练习 smoke 在 Windows 暴露平台假设失败；改用原固定 Linux 镜像、只读挂载实服源码重跑 34 项全过，归档旧报告后发布新证据，练习探针通过。
- 重启后发现 NPC 依赖的 4 个原生 HTTP 连接 inactive：桐人的 `qd_party`、两人物的 `maid_native`、结衣的 `qd_party`。通过新的 `skill-system-reconnect-20260920` 空闲维护边界，官方 API 同值保存重连，3/7/7/3 工具读回、配置和权限全等。原 `numen_survival` 已正常，未重连；未再次重启服务，原角色和 16 Cron 再次精确恢复。以后应在依赖服务已恢复后同步角色技能，并检查每个实际原生连接，不能只检查 Docker healthy。

上述维护脚本已完成一次，不得重放 capture/pause/deploy、QA 发物品或旧身体恢复。新的改动需要新的维护 ID、独立证据及精确部署前像。
