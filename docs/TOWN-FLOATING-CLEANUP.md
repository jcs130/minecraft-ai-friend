# 主城悬空残块清理

2026-09-20 23:02（北京时间）已在生产完成。用户指出旧城区有异常悬空方块；本轮检查重建后的真实存档，清理 **1,245 个残块**，将 **2 盏新路灯重新落地**。最终保存世界逐格核对 1,256 个变化坐标通过，城区及矿道周边复扫未发现本轮定义的低空孤立残块。完整空岛楼阁保留。

## 范围与判断

正式备份为唯一离线读取源，扫描 X[-800,-359]、Z[679,1051]、Y[48,319]，覆盖 672 个区块。仅报告与 X[-785,-375]、Z[695,1035] 相交的组件，外围作为连接判断上下文；底面 Y48 和扫描侧边作为连接锚点。这不是全世界或地下结构扫描。

先识别六面连接组件，再用包含斜角的 26 邻接保守复核，避免把斜生树枝和屋檐误当垃圾。144 组具有斜角连接的结构保留。对真正不接地的低空组件逐组检查材料、坐标和方块实体，最终确认 53 组、1,251 格，其中 6 格属于需要移位的两盏路灯。

清理对象为断树冠/树干残段、孤立火把、土块/木板、25 格橙色羊毛悬台，以及四柱各 7 格的旧白灯。没有按高度或大矩形批量填空气，也没有删除或搬动带物品数据的方块实体。西岸模组设施保留区内有 41 格无实体的孤立树冠，依据本次清理要求处理；此前重建阶段的“保留体积零变化”是当时验收，不能当作本轮后的绝对不变承诺。

高空完整楼阁及其附属树木约 5,051 格，含功能方块，属于完整结构，未按残块拆除。保存前后 5,051 格完整方块状态一致。该结论来自实际存档与方块投影，不冒充第一人称客户端视觉验收。

## 两盏新路灯的实际修正

坐标均为灯柱最下方方块；灯柱由两节云杉栅栏和一盏灯笼组成。

| 原位置 | 最终位置 | 依据 |
|---|---|---|
| (-726,66,898) | (-726,64,898) | 地面草方块在 Y63，原灯柱悬高两格 |
| (-562,68,880) | (-562,68,883) | 原点下方为水；向南移三格到 Y67 草地，保留水体 |

第二盏的两节栅栏南侧分别邻接泥土和草方块，因此原生邻居更新后 `south=true`。后续布局应使用真实地面支撑与原生派生连接状态，不能只检查模板写入坐标。

## 执行与独立验收

复用既有有限数据包的前像/占位/单次锁/后像模式，没有新服务、自动清扫循环、角色提示词或 Agent 权限变化。施工前检查 6,433 格完整状态、53 个已加载区块及 1,256 个目标格的实体占位；预检冲突为零，同步施工入口再次检查后只执行一次。运行中没有停止服务、冻结 tick、传送或删除玩家/生物，也没有物品、交易、人物档案或调度修改。

首次 `apply` 返回 0，永久 `#attempt=1` 保留，**没有重跑**。现场 1,256 格逐项核查发现，1,254 格匹配，另外两格仅为上述栅栏正常南向连接。正式中间备份独立读回确认附近土坡、实际连接及未施工邻格均符合物理状态。保留原 `#post=0`、原清单和失败回执，另生成明确包含两项派生连接的只读验收函数；原生返回 1，`#verified=1`。这不是抹除失败或扩大宽松比较规则。

最终正式备份独立检查：

- 1,256 个坐标与解释后的完整最终状态严格相等，两盏灯直接接地。
- 原 53 组低空孤立残块复扫为 0；144 组原有斜角连接组件保留。
- 5,051 格完整空岛状态无变化，未施工邻格无变化，施工目标无方块实体。
- 53 个本轮拥有的 forceload 均逐个释放，原先无强加载，最终查询也为空。
- 服务器仍正常 tick；既有主城保护四项健康检查通过，保护范围和 JAR 不变。

世界和精确坐标施工包留在本机私有运行目录，不把存档、库存 NBT 或凭据提交到 Git。公开汇总见 [floating-cleanup-verification.json](../assets/town-expansion/floating-cleanup-verification.json)，布局修正见 [plan.json](../assets/town-expansion/plan.json)。

## 本机证据与恢复约束

证据根目录：`runtime/town-floating-audit-20260920/`。

| 证据 | 文件/正式快照 |
|---|---|
| 清理前正式备份 | `backup-before.json`；`2026-09-20T144144.0821556Z-b2b1dab1` |
| 原始扫描及保守复核 | `scan.json`、`diagonal-review.json`、`points.json` |
| 实际候选与安装哈希 | `plan-v2.json`、`package-files-v2.json`、`install-receipt.jsonl` |
| 单次施工与逐格诊断 | `apply-once.json.journal.jsonl`、`diagnostic.json.journal.jsonl`、`check-partial.json.journal.jsonl` |
| 中间正式备份和两项派生状态 | `2026-09-20T145753.4581891Z-78c940c0`；`partial-verification/saved-verification.json` |
| 独立原生验收及临时区块释放 | `accepted-final-states.json`、`verify-landing-install.json`、`finish.json.journal.jsonl` |
| 最终正式备份 | `backup-after.json`；`2026-09-20T150041.3973626Z-a93524d3` |
| 最终保存世界复扫 | `final-verification/final-result.json`、`saved-verification.json`、`scan.json`、`diagonal-review.json` |

三个正式备份均 complete 且 save-on 已确认。本轮已经结束；不得清锁重投施工，也不得恢复旧快照覆盖随后玩家活动。未来新异常应读取新快照并单独审查。此项局部验收不覆盖任意第三方模组直接写块，也不改变此前全局健康验收的历史失败状态。
