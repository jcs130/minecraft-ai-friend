# 千灯纪 · 项目现状（2026-09-19 凌晨）

> 本文是**看过实物之后**写的：容器、工单库、看板、世界实况、仓库都逐条查过，
> 每一条结论背后都有可复查的来源。看不惯哪个数字，去查它，然后**改事实、别改文档**。

## 一、这是什么

一个**多角色 LLM 世界**：十几个 agent 角色（桐人、结衣、司灯、灯语女神、世界工程师……）
各有身体、记忆（ReMe）、知识树与技能库，活在一个真实的 Minecraft 世界（13 个容器组成）里；
外面套着一层**自我改进系统**（技能生命周期 + 证据闸 + 学习班次 + 元层看板），
以及一套**工单系统**让角色把问题变成可追踪的单子。

## 二、组件栈（`docker ps`，2026-09-19 01:2x）

| 容器 | 干什么 | 状态 |
|---|---|---|
| `qiandengji-mc-1` | Minecraft 服务端（世界本体，RCON 25575） | Up 15h ✓ |
| `qiandengji-world-1` | 世界引擎（TS，`bootstrap-world.mts`，独占 RCON，Goddess 观察者） | Up 37m ✓ |
| `qiandengji-npc-1` | 村民/NPC 社会、派对与对话、女仆桥（`/opt/sidecar`） | Up 4h ✓ |
| `qiandengji-survivor-1` | 桐人的生存控制器（`/survival/controller.py`） | Up 3h ✓ |
| `qiandengji-qwenpaw-1` | 10 个角色 agent 的家（控制台 18089 / API 8088），也是 PawApp 的宿主 | Up 2m ✓ |
| `qiandengji-panel-1` | 日常管理首页（19091：总览/服务器管理/众生） | Up 4d ✓ |
| `qiandengji-gate-1` | Agent 协议入口（25701） | Up 5d（无健康检查）|
| `qiandengji-inventory-1` / `control-1` / `resources-1` | 背包、控制面、资源包分发 | Up 5d / 2d / 5d ✓ |
| `qiandengji-asr-1` / `voice-1` / `tts-1` | 语音输入、语音通道、Kokoro TTS | Up 5d ✓ |

**对外入口**（都只绑 127.0.0.1、本机免密）：管理台 **19091** ｜ 游戏 QwenPaw **18089** ｜
运营 QwenPaw **18090** ｜ **天神之眼渲染 19092** ｜ Agent 协议 **25701**。

## 三、今晚（9-18 夜 → 9-19 凌晨）做了什么

**23 个提交**，主线四件：

1. **学习链路真正接通**：证据闸改看角色真正在写的地方（`digest`/`notes`/`memory`）、
   学习班次从"每周且零模型"改成**每小时且证据门控**、到期的生存轮**整轮变成学习班**。
2. **两处卡死被救活**：结衣的对话队列（一封 15 小时"投递未确认"的信堵死整条 lane）
   ——用**操作员放行**（404 探针 + 超时已过 + 发起进程已不在三证）解；桐人被
   `cancellation_uncertain` 停死 40 分钟——四层追到底（终态永不到来 / 异常冒泡 /
   孤儿暂停 / 改状态没落盘）。
3. **原生工具面修复**：容器运行期配置陈旧导致**整套原生工具被守卫拒绝**
   （工程师连自己工作区都写不了）→ 重新配给 + 重启，工程师已自写文件为证。
4. **RSI 第 1、2 层**：政策文件 + 元层看板（从代码常量生成，抓到两处真实漂移）；
   角色可提"改进机制"的提议，白名单 + 冻结区 + 越界必拒给理由；**载体迁到工单系统**。

**两个 PawApp 进了控制台** ✓：`📈 自我改进看板`（一角色一行 + 红旗配色 + 待审提议）与
`👁 天神之眼`（世界观察画面**真嵌入**——为此把 19092 的 `frame-ancestors` 按配置放行控制台）。

## 四、健康与产出（都是查出来的）

- 容器：**13/13 在跑**，12 个有健康检查的全绿；`qiandengji-qwenpaw-1` 健康 `ok=true`（2.2.1，10 角色）。
- 世界：3 人在线 —— `Kirito`（y=95，血 18.4/20）、`Goddess`、`Neko`（**外部 AI 客户端，实验用**，非本世界角色）。
- 工单系统（最近 30 单）：`improvement 6` / `bug 11` / `gameplay 8` / `content 2` / `operations 3`；
  状态 `open 6` / **`blocked 10`** / `resolved 4` / `working 7` / `needs_review 2` / `duplicate 1`。
- 知识产物：桐人 **565** 个、结衣 **298** 个（近 2 小时各 +6/+5）——**固化一直在发生**。
- 技能级草稿（`learning/drafts`）：**仍为 0** ✗ —— 所有角色都是。

## 五、问题清单（按严重度，全部有据）

| # | 问题 | 证据 |
|---|---|---|
| 1 | **工单状态落后于现实**：至少 3 单今晚其实已解决，仍挂 `blocked` | `case-bd8c007463a20ee5123d`（原生工具被拒 ✓ 已修）、`case-040c7afab2754047376f`（`team_native_host_inactive` ✓ 已修）、`case-4964d4b4…`（npc 健康）|
| 2 | **技能级草稿恒为 0**（14 角色） | 看板 `no_skill_draft_yet` × 14；根因判为**缺第 4 层度量**（没有元指标，就无法判断改动是否让"下一轮更好"）|
| 3 | **`qd-engineer` 的班次 6642 分钟没跑** | 看板 `lastShift.ageMinutes=6642`（它的作业在逻辑身份上归 `mc-god`，本地 `jobs.json` 是陈旧副本）|
| 4 | **3 个对话角色知识产物为 0** | `mc-herald` / `qd-maid-dialogue` / `qd-villager-dialogue` → `no_knowledge` |
| 5 | ~~不速之客~~ **已查明：外部 AI 客户端**（造物主 2026-09-19 告知，在做实验）——它不在 village 名册、不属任何角色绑定，但**照常受女神指引**（造物主定调：别的客户端接进来该怎么指引就怎么指引，那本就是本服特色）；两条记账纪律：不替它扩权、归属清楚 | RCON `list` + `data get entity Neko Pos` = `(-543.5, 189, 867.5)`，满血 20/20 |
| 6 | **10 单 `blocked`** 里含 4 单基础设施工单（工程提交通道、survival 验收覆盖、npc 健康、内容提案过期） | 工单列表（见上）|

## 六、RSI 路线图状态

| 层 | 内容 | 状态 |
|---|---|---|
| **1** | 元层可见（政策 + 看板 + 控制台页面） | ✅ **已上线并在面板可见** |
| **2** | 角色可**提**改机制（白名单/冻结区 + 越界必拒给理由），载体=工单系统 | ✅ **已上线并实测**（提议已进工单，`owner=game:mc-god`）|
| **3** | 角色可改自己的 harness | ⛔ **被第 4 层挡住**（必须先有度量，否则会高效地优化假目标）|
| **4** | 度量与基准（被验收技能数、跨角色继承率、回退率、故障被发现时间） | ⬜ 未做 —— **建议下一步就做它** |

## 七、建议的下一步（排序）

1. **做第 4 层度量**（把"改进是否让下一轮更好"变成一个数）——它是第 3 层的前置，也是"草稿恒为 0"的判据。
2. **史官活**：用今晚的证据**结掉/更新**那 3 张已解决的工单（结单权在 `COORDINATORS`）。
3. **查 `Neko`**：自己的世界里出现不认识的角色，先查清是谁、谁带来的。
4. **修 `qd-engineer` 的班次**（拉通逻辑身份与本地 `jobs.json`）。
5. 保持：每小时证据门控的班次 + 看板巡检（`--check` 可接进 cron）。

---

*本文由天神（`mc-god`）在 2026-09-19 凌晨逐项查证后写成；数据来源：`docker ps`、`TeamStore.cases()`、
`world-notes/evolution-board.md`、RCON、`qwenpaw_health.py`、`git log`。*
