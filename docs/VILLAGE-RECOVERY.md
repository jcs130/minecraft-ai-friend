# 主村庄恢复与禁刷修复

2026-09-20 已实际修复现役 `D:/Projects/QiandengJi/server/mc/shadow`，不是旧 CoPaw 世界。用户要求主村为不刷怪的安全区，并授权直接修复、部署及提交 main。

## 现场与原因

最初主村半径180格只有3名村民、4只铁傀儡。铁傀儡仍活着，位于Y32～56的地下，并非需要重新召唤。禾叔当天12:00:54被僵尸杀死；前一天通宝、墨先生、烛九也有僵尸或掠夺者击杀记录。日志没有给出每只凶手的生成来源。

InControl原配置正常加载、村庄坐标正确，但仅有默认`position`入口，垂直范围Y25～125。按现役1.21-10.2.7 JAR核对后，已保留原XZ范围，扩大至Y-64～320，并为敌对生物及原幻翼条件明确配置`position`、`finalize`、`onjoin`。源文件在`config/incontrol`。实际重载使用现有Goddess的ServerPlayer命令身份，没有传送、调用模型或重启MC；16:21:04日志确认重新读取规则且无解析错误。

另有40只精确UUID、类型和实时位置均确认的残留敌对生物，其中34只在旧范围Y25以下。已在命令中同时限定原身份、类型、区域、无自定义名字及非持久化条件清理：17僵尸、11骷髅、9苦力怕、2蜘蛛、1幻翼。未加载、未知模组、宠物及可治愈村民不在清理集合。

这些规则阻止区域内匹配的生成与加入世界，不是持续的移动边界或伤害屏障；不能由本次结果宣称外围怪物永远走不进来。

## 恢复范围与身份

全维度70个实体区域核对了备份中33名具名主村村民的UUID，包括所有实体类型，以排除异地存在或转化。3名存活、30名缺失，无异地或重复身份。恢复依据是实际实体备份，未照35份旧角色配置批量补生；云笈、锄婆没有这批实体备份证明，未凭空生成。

- 30名缺失村民使用原UUID恢复，保留备份职业、交易、随身物品和其他持久数据。5名缺失公会角色的职业/交易/物品使用较晚的9月8日绑定快照；未改公会账本、任务进度或Agent记忆。
- 原来3名村民保留身体，补齐保护并恢复血量；岚被模组改成Maricruz的名字已恢复。4只现存铁傀儡只移位，不新增，血量保持原值。
- 37个不同落点逐一验证3×3实心自然地板、上方3格空气、加载状态及无实体占用。全部村民保持原生AI，具名角色使用原NPC设计已有的防伤害与持久化保护，不靠NoAI站桩。
- 恢复后33名村民及4只原铁傀儡均实时读回；后者已在地面自然移动。`save-all flush`明确返回`Saved the game`。

注意：名为9月20日的备份目录实际包含9月7日世界内容，不代表恢复到死亡前一刻；9月8日以后的交易变化无法据此完整重建。源SNBT、来源时间、文件哈希、逐命令回执及读回均保留于忽略目录`runtime/village-recovery-20260920`，没有用旧世界整体覆盖现役存档。

## 持续修复与部署

`mc_npc.py`现有`heal_npcs`和`summon_npc`均阻止任何绑定身份进入旧随机UUID补生分支，包括损坏绑定。否则打开旧补生开关会不断创造查询不到的新身体。`spawn_missing=false`继续保持。

原`unleash_alive`启动流程现在为通过校验、精确UUID+原版村民类型+角色标签匹配的绑定角色补齐`Invulnerable`和`PersistenceRequired`，同时开启AI；不移动或补生。未绑定角色保持原启动行为。

本轮仅一次停止/启动NPC容器；先暂停原NPC提交入口、排空桐人并暂缓三个直接依赖NPC的学习Cron，确认公会无待执行事务。原准入、桐人开关及三个角色全部Cron已精确恢复，未清理旧未知任务。MC、Qwen、survivor未重启，未创建新守护服务。

操作中一个`PortalCooldown:0`合并返回“值已相同”，已读回核实而未重投召唤；原版NBT在AI启用时省略`NoAI`，验收采用“原实体存在且不匹配NoAI:1b”，不把字段缺省当失败。标签按集合核验，交易列表仍按顺序与内容核验。

## 验收

- 24项固定镜像、源码只读、禁网回归通过：绑定保护/拒绝重复补生、旧别名及安全区范围/入口。
- 实服Y0、72、150、319四个受控僵尸加入测试均未产生目标UUID，后续再次查询仍不存在；没有重复提交测试。命令文字可能说“Summoned”，以实际身份读回为准。
- 复查区域内13类常见敌对生物均未检出。镇外边界区块没有加载，未为测试强制加载；镇外放行由配置契约测试覆盖，未冒充实服对照试验。
- NPC健康通过，岚和禾叔实时在线；路由6项、15路由/15站点通过。面板相关Qwen、桐人、具身、实践及PawApp探针通过，无新增失败；13项历史全局异常仍保留。

备份脚本还发现世界、RCON端口与凭据路径均停留在旧目录。修正应使用当前项目的RCON客户端、独立新目录、明确退出码和`finally save-on`，不混写或自动删除历史备份。当前Windows身份无法读取`MCWorldBackup`任务定义，不能宣称已验证其自动调度。

配置或源码需要回退时使用本轮保存的逐文件前像，保留已经恢复的原身份和当前游戏进度；不要重跑本轮召唤脚本、启用旧自动补生或回灌整个旧世界。
