# 桐人、结衣持续运行修复（2026-09-21）

本次处理两个已停滞的原角色，保留身份、世界进度、会话和原有模型配置。恢复运行与游戏目标完成分别验收。

## 9 月 21 日晚：缺失装备导致暂停

21:28，桐人请求装备背包中不存在的铁剑，动作 `1de6ae97d6b7458187241221e11f135a` 进入 `action_outcome_unknown`。本次身体位于工作区内，不是下午的越界问题。

Numen 的 `EquipCompanionTask` 会将缺材料判为失败，但 `TaskDispatch.runSync` 不通过 RCON 调用的回调返回终态；`numen_act invoke` 只返回 `accepted:true`。旧网关轮询五秒看不到目标装备，便按真正的不确定写入处理，关闭自主循环。服务仍有正常心跳，原健康检查没有识别这种阻塞。

修复使用已有的实时背包快照，在分配动作 ID、消耗租约和发送原生命令之前检查物品。不存在时返回 `equipment_item_missing / dispatched:false`，允许模型在原回合换装备或先获取材料。已经派发但无法确认的动作仍保持未知保护，不因超时自动重投。具身健康探针新增 `action_outcome_known`，同时检查未知标记、未知租约和该暂停原因；人工暂停不算这类故障。

旧动作没有可追回的装备终态。操作员核对原生认知任务 `task-5f524a87be6e` 已完成、原始工具回执身份匹配、两次新鲜且递增的身体 tick 均为空闲、铁剑在动作前和当前都不存在后，将阻塞记录为 `operator_retired_unverified`。原始未知回执和动作日志保持原样，归档证据后关闭旧租约，未重放、未计成功；之后通过原控制入口恢复自主循环。

源码与生产各 226 项隔离具身/网关回归通过，生产原生 MCP 工具 49/3 与结衣 7/3 可读，完整 QwenPaw 健康通过。仅通过管理器重启 survivor，原 10 profiles、16 Cron 配置和准入恢复。恢复后伙伴对话 `task-738e1135244e` 已取得游戏 `heard` 回执，随后启动自主任务 `task-890bcb476f0e`。现场后续验收见本机 `runtime/kirito-equipment-20260921/`；一次性对账已完成，勿重放。

本次堵住“缺少装备”这一确定可预检的原因。RCON 同步槽终态传输缺口尚未整体改造，其他真实未知动作仍需对账；下午越界回位问题也不是本次补丁修复的范围。

23:04 的现场验收已覆盖原触发条件：自主任务 `task-890bcb476f0e` 再次调用 `equip`，实际返回 `equipment_item_missing`，随后仍能查询身体、尝试进食和合成，最终原生任务 `finished/completed`，控制器进入下一次伙伴对话 `task-d3fdca328613`，未知标记没有重现。进食因饱食度已满返回明确失败，铁剑合成因附近没有工作台被拒，工作台放置被既有施工区域规则拒绝；这些均不冒充目标完成或成功移动。

生产 34 项实践测试重跑通过并更新真实报告；具身、实践、桐人、QwenPaw 等相关健康通过。完整 panel-smoke 仍有其他历史来源/管理/技能等检查失败，未宣称全局全绿。原女神班次 23:03:32 自然执行成功并排到 23:11；运行中 Cron 的 `next_run_at` 可能在收尾前保留旧值，应结合原生历史判断，不能仅凭短暂过期反复重新登记。

## 故障与修改

- 桐人的 `poll_model` 原先把查询已有模型任务时的任何异常都当成结果未知，立即暂停自主控制。旧任务 `task-34e839dfcd20` 实际已在 QwenPaw 完成，控制器也记录了收尾，但暂停状态仍保留。旧记录没有异常类型，无法追认它一定是网络超时。
- 现在仅对网络传输异常及 HTTP 408/429/500/502/503/504，在原任务期限内退避查询同一个 task。等待信息持久化，重启不重投模型请求，不因一次 GET 失败关闭仍在运行的模型的身体工具。404、鉴权失败、未知格式和期限耗尽继续保守处理；人工暂停仍优先。
- 结衣被 9 月 18 日的 `task-48e70c920187` 占住任务通道。核实精确任务返回 404、角色无运行任务、原生运行环境已重启且 NPC 写入进程停止后，复用现有 `operator-reconciliations` 机制释放通道。旧任务仍是结果未验证，没有改成成功或重放；另将从未提交的旧提示预留归档，等待新的生活信号。
- 恢复后的游戏实测发现公会讨伐计数器创建命令把 `minecraft.killed:minecraft.zombie` 等统计条件误写成 `minecraft:killed:…`，四项 objective 均不存在，导致桐人到柜台仍无法接单。修正四个条件，并用原生命令创建缺失计数器；未设置、清零或补造玩家分数。旧运行函数立即能读取新计数器，修正后的创建代码也已同步生产目录。

## 部署与恢复

使用原维护流程暂停入场和班次、确认所有原生任务闲置后，仅通过 control 的 plan/execute 停启 `npc` 与 `survivor`。没有重启 Minecraft 或 QwenPaw。原 10 份角色配置和 16 项 Cron 精确恢复，原关闭的工程、女神、设计班次保持关闭；桐人的原暂停状态经终态核实后显式恢复。

依赖服务启动后需实际读取 MCP 工具清单。此次 `numen_survival` 的熔断状态未被同值白名单更新清除；QwenPaw 2.2.1 的驱动刷新不等于连接重建。维护中完整 Client DTO 写回还丢失了自定义凭据别名，已恢复原 `env:SURVIVOR_MCP_TOKEN` 引用并验证卡片，随后用官方 `PATCH /api/mcp/toggle/{key}` 关闭再开启原连接，保留其配置与权限。五条相关连接均通过工具读取验证，Numen 为 48 个工具。

首次 start 操作因连接未及时恢复而留下 `readiness_timeout` 失败回执；保留该回执，后续连接修复及实际健康检查另行记录，不重写历史成功。

## 验证与边界

- 新增 5 项模型轮询恢复测试：同任务恢复、不重复提交、重启后的原期限、人工暂停、临时 HTTP 错误与永久错误区分。
- 新增 2 项公会计数读取测试：原生空分数和已有分数可作基线，缺失 objective / 传输失败仍为未知。
- 73 项原控制器回归、9 项女仆对账测试通过；最终具身冒烟从 82 项扩为 84 项，在生产目录实际字节上重新执行、记录来源哈希。测试容器只读挂载且关闭网络。
- 实服 01:43，桐人 `task-dc1c27c39886` 正常完成，实际移动到接待员附近；首段导航失败、后段完成和接单拒绝均有独立回执。后续自主任务 `task-a6a04642c812` 因重复接单触发原生循环保护，保留失败，没有计作成功。计数器修复发生于其后。
- 结衣 `task-bf9337768589`、`task-ec35c2e60eeb` 连续完成，新任务由原调度启动；使用真实身份、身体上下文及游戏内交流工具。
- 01:50 原班次再次自动启动桐人 `task-8a24a459b3f2` 并正常收尾，未再次暂停；结衣随后还有 `task-4ceb9f0de734`、`task-2faf4308ea51` 完成。结衣首轮发言有原生 `nearby_speech_heard` / `heard:true` 回执，语音排队不冒充播放完成。桐人该轮仍依据旧工单等待修复，没有重新接单；因此计数器原生修复已验证，修复后的自主接单成功尚未验证。
- 13 个活动服务、12 个要求健康的服务均正常；桐人、具身循环、QwenPaw、PawApp、主城保护与技能系统相关探针通过。完整 panel-smoke 仍有旧来源证据失配、伙伴历史身份/运输证据及其他运营检查未通过，不将全站标为全绿。

本机原始证据在被 Git 忽略的 `runtime/agent-continuity-20260921/`：维护快照、停启回执、旧任务核实、连接恢复、测试记录、实际任务及连续观察。该目录含私有角色数据，不纳入源码。维护已恢复，勿重放恢复脚本或删除未知回执。

此修复证明循环恢复、真实工具可执行，以及若干新任务已收尾；不承诺无限在线、每轮游戏成功或已经验证 RSI 收益。永久丢失的任务仍需证据对账，不能用自动重投掩盖。
