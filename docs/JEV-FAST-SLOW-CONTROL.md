# Jev / QwenPaw 快慢控制

2026-09-21。部署验收记录见本文末尾。代码沿用 survivor、SkillLibrary、PracticeStore、QwenPaw 原生任务和 Numen 身体网关。

## 控制分工

| 执行层 | 工作 | 触发方式 |
| --- | --- | --- |
| Numen 原生任务 | 导航、移动、交互及游戏 tick 内的执行 | 接受一次有界动作后持续运行，查询原任务回执，不重复派发 |
| 已测试程序 + Jev | 程序处理确定性步骤；有多个合理候选时让 Jev 分类 | 身体空闲且上一动作已结算，构造当前状态与完整候选；单个异步请求 |
| QwenPaw 认知 | 小目标、技能程序、前置与终止条件，失败后的规划与复盘 | 目标变化、程序完成/失败、低置信、服务不可用等现有触发 |
| QwenPaw 交流 | 回应已听见的信息，使用独立的只读交流会话 | 与程序、Jev 等待和原生动作并行；不能借身体租约行动 |

程序返回的 `choose` 新增可选 `context` 对象，最多 2048 字节，用于任务局部事实，如距离、可见障碍、上次动作后的实际进展。调用方应核对感知来源、新鲜度；没有自动拷贝整个场景、聊天历史或思考过程。控制器同时提供候选的确切 tool/args，避免仅用模糊文字描述动作。总状态仍限制在 8192 字节，候选仍为 2–8 个。服务没有会话 KV 增量协议，因此每次仍发送小型当前快照，不声称实现了跨请求缓存。

分类目标优先取本次实践的 `objective.description`，没有局部目标才取长期记忆目标。实地发现原适配器在“准备工作台材料”时仍传入“击杀骷髅”，因此补上这一优先级及回归测试。输入正确不保证置信度或行动质量提高，仍须结合实际候选和当前身体核验。

交流沿用独立的只读 session。新目标先获得行动规划机会，空闲时已有一次交流且规划到期则让出下轮；原生动作或技能正在执行时仍可聊天。具身模式的身体规划不再领取队伍消息，避免不断被归成交流会话。保留原角色并发与调用预算，不增加第二套 Qwen 守护进程。

`policy_worker.py` 只做推理，没有 gateway、租约和写盘接口。一个工作线程、一个在途槽、一个长期 HTTP 客户端；忙时不能排第二个请求。只有主控制器能复核并执行结果。等待不推进程序 memory/steps；重启后重新观察和分类，不恢复旧选择。已经派发的动作仍按原 dispatching 标记、原生任务 ID 和回执处理，未知效果不重放。

结果绑定提交时的完整技能作业、目标、记忆代和控制意图。接收时复核身份、维度、原生任务、库存、装备、生命状态与位置（允许不超过 0.5 格的被动位移）。暂停、维护 drain、离线、原生任务变忙或慢系统取得身体时丢弃待接收结果。分类超过 5 秒或身体前提改变，最多重新观察两次；再失败交回慢系统。网络忙槽也有 5 秒等待上限，不能永久卡住自主运行。

主服务在等待分类时目标轮询间隔为 250 ms，程序/原生动作/交流活动时为 1 秒，其他情况保留配置间隔。这些是调度目标，完整观察与发布有自身耗时；不是 4 Hz 模型调用保证，更不是游戏物理频率。身体仍由 Numen 连续运行，暂未增加通用 WASD/鼠标输入片段，也未实现原生任务执行中的 Jev 抢占或多问题批量分类。这些要先有任务取消/所有权和独立场景证据再扩展。

## 80 ms 与本机测量

[官方发布说明](https://typesafe.ai/blog/introducing-system-one-models-and-jev)给出的端到端范围为约 70–500 ms，并说明其测试主要在服务所在的美国西海岸进行。80 ms 不是任意地区、任意输入的延迟保证。本机不能消除跨地区网络往返。

本次从实际 survivor 容器调用官方 `jev-latest`（返回 `jev-1.13.0`），每次重新读取真实身体，只运行影子分类，未派发动作。交替执行冷连接与持久连接共 7 对，排除第一对预热后各 6 次；1929 字节状态、1180 输入 token、33 输出 token，模型与请求结构相同。

| 连接方式 | 中位数 | 最小–最大 | 样本 |
| --- | --- | --- | --- |
| 每次新建客户端与连接 | 816.55 ms | 636.04–1072.36 ms | 6 |
| 同一个客户端复用连接 | 278.50 ms | 262.78–321.17 ms | 6 |

这组小样本中位耗时下降 65.9%。冷请求 TLS 阶段约 325–646 ms；复用样本均未重新建连。分阶段计时只保存事件名与时间，不保存 HTTP 头和认证。`responseHeadersMs` 包括网络与服务端处理，**不能当作模型纯推理时间**。首次连接或闲置超过 60 秒仍可能重新握手，无额外付费心跳或隐式重试。

这些样本均选择/触发回慢系统，不作为策略质量或游戏任务成功证据。分阶段原始计时见 [测量记录](research/jev-fastslow-latency-20260921.json)。公共状态现在可显示 provider、状态字节数、连接/TLS/HTTP、请求到接收耗时与等待接收耗时；避免把 API 时间混为完整动作时间。

## 验收

最终源码与生产字节各通过 116 项具身冒烟；生产另通过 34 项实践冒烟。此前 73 项控制器、14 项快技能回归通过，部分集合重叠，不能相加成独立测试总数。新增回归覆盖阻塞网络期间交流可用、单请求、旧结果作废、过期/忙槽有界退回、连接复用、交流让位与当前实践目标，以及原租约和回执路径。最初一项测试用 2 秒自动释放模拟阻塞，慢速测试准备阶段超过该时间导致失败；改为显式事件控制并隔离无关读取后通过。首轮源码冒烟执行期间文件变动，110 项虽通过但 hash 验收失败，原报告保留，最终重跑成功。

生产验收：三次部署均只由原管理器 stop/start survivor，最后于 12:10:35 启动健康；没有重启 Minecraft 或 QwenPaw。首轮 9 个保留晋升版本 62 条 fixtures、第二轮包含新版本的 11 个版本 74 条 fixtures 均实际重测通过，未更改晋升历史。最后仅改控制器目标取值与测试，技能内核未变，保留已有真实报告。角色指南通过原生 Skill API 同步；每轮恢复 10 个 profile、16 条 Cron 和 NPC 准入状态，原禁用班次保持禁用。桐人 48/3、结衣 7/3 个原生/队伍工具可用；Jev、具身和实践相关 panel 检查通过。完整 panel 仍有既有来源/资产/部分历史验收不匹配，整体不是全绿。

初次指南同步因隔离源码缺运行角色清单而在写入前退出，随后使用实际部署的清单和官方 API 完成。最后报告发布脚本误读实践报告的计数字段，修正读取 `execution.testsRun` 后发布，原测试结果未改。恢复时仍有新进入的 mc-herald 原生任务；本次只改 survivor，因此只要求桐人自身空闲，保留该语音任务继续运行，再按原 API 恢复调度，没有取消无关任务。

## 实地结果与未完成的验证

维护者通过原目标入口给出一次补给实践和一次基于失败回执的修订反馈，由桐人实际调用 draft/test/promote/start，同名 `prepare_for_task` 的历史版本保留。这属于有人指导的实践，不能算自主 RSI 收益。

- 首个新版本 `eeb5fd…` 的实践 `0db688…` 无可执行候选而结束，零身体动作，补给目标未达成。
- 修订版本 `1ab860…` 的实践 `eb7c78…` 开始后，一次身体前提变化令分类作废；下一次官方 Jev 选择 `craft_planks`，概率 0.57、confidence 0.13，按既有阈值交回慢系统。没有放宽门槛、重放动作或重置失败记录。
- 这次真实 HTTP 为 613.38 ms（冷连接、TLS 300.90 ms）；请求到主循环接收为 1643.93 ms，其中结果等待接收约 1030.20 ms。主循环还有身体快照/发布等 IO，250 ms 调度目标不等于实际接收延迟，尚未证明稳定的高频闭环。
- 随后原 Qwen 任务 `task-df18f3616643` 接手：原木 −1 / 木板 +4，接着木板 −4 / 工作台 +1，两个合成均有成功回执；面包合成被拒绝，持有工作台不等于附近已放置工作台。后续另有导航回执，补给仍未成功。
- 对长期目标/局部目标做过四次额外影子分类，但此时慢系统已改变库存，候选变成面包，不能拿来证明目标修复提高准确率；保留原结果，目标传递修复由回归测试验证。

本轮没有新增官方 Jev 的“选择—派发—确认成功”动作链。原历史本地 Decider 的两条成功链仍只归属本地基线。临时验证 mission 已恢复原空值，自主运行恢复；当前记忆、技能和练习记录没有回滚。接下来需要在自然发生或独立场景的多步任务中比较完成率、重规划次数、总耗时和费用，不能以连接提速代替策略收益。

维护证据在被忽略目录 `runtime/jev-fastslow-20260921/`、`runtime/jev-fastslow-fairness-20260921/` 和 `runtime/jev-fastslow-goal-20260921/`。维护均已结束，一次性脚本禁止重放。
