# mc-world:2.1.5 — 2.1.4 + 面板「服务器特色」卡：任务板（公会委托）+ 修为榜 + 法则技艺谱
# 2026-08-26：apiState 增 questBoard（guild-<today>.json 全板 + guild-fame + 近7日流水统计）
# 与 atomTable（36 法则技艺）；/api/village 任务读改走 MC_QUEST_DIR（mcdata 卷，修复
# 此前 panel 未挂 mcdata 导致委托信息静默为空的暗病）。compose 同步挂 ./mcdata:ro。
# 用法：docker build -f Dockerfile.mc-world-inspect -t mc-world:2.1.5 <repo 根>
FROM mc-world:2.1.4
COPY web-panel.mjs /app/web-panel.mjs
