# Contributing

这个项目目前处于 MVP 阶段，优先保持本地运行简单、功能边界清晰、安全默认值保守。

## 开发

```powershell
npm install
npm start
npm run check
```

## 提交前检查

- 不提交 `data/`、`logs/`、`.env`、服务器世界目录或 API key。
- 运行 `npm run check`。
- UI 文案优先中文，开发者文档可以中英混合。
