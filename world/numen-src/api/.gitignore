# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea/*
!.idea/scopes

# vscode
.vscode/

# gradle
build
.gradle

# other
eclipse
run
runs
logs
.vfox

# datagen output — regenerated via ./gradlew :fabric:runDatagen + :neoforge:runData
**/generated/

# Claude local tooling (skills, settings, etc.) — not mod source
.claude/

# local dev scratch (mock MCP servers used for manual testing, etc.)
dev/

# benchmark credentials — API keys live in an untracked .env (see benchmark/.env.example)
.env
**/.env
!**/.env.example
