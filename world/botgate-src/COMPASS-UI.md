# 技能罗盘接口与验证

2026-09-20 当前为原版 `GENERIC_9x6`，54 格。旧 `custom_data.skillbox=true` 指南针与 F6/`skillchest self` 入口保留，普通指南针不接管。默认 `known`；已学女神秘术与实际装备原生法术显示一次，未学内容通过学习图鉴进入。

| 位置（0 起） | 功能 |
|---|---|
| 0–6 | 我的技能、战斗、辅助、移动、生活、被动、铁魔法 |
| 7 / 8 | 入门指南 / 传送阵 |
| 10–16、19–25、28–34、37–43 | 每页 28 项技能，边框留空 |
| 45 / 53 | 上页 / 下页 |
| 46 / 47 / 48 | 历史档案 / 快捷栏 / 我的技能 |
| 49 / 50 / 51 / 52 | 状态 / 刷新 / 学习图鉴 / 关闭 |

传送阵和造物每页 36 项；共享/私人地点仍用稳定 ID，菜单不重写存档。指南支持 `skillchest guide <玩家> [页]`、统一 CLI `menu guide`。普通玩家仅可打开自己，代开他人需 OP。

目录与快照来自既有 `/mcdata`。主题别名优先检查原生装备，不再用旧 learned/level 锁住已装备法术。不具备装备、魔力不足、正在施法或冷却中的项没有派发动作；灰色点击不关闭界面。可尝试学习的秘术仍由原施法服务验证条件并收录，点击并非免费解锁。已解锁被动识别 atom ID 与旧 passiveId 两种记录。

说明按中英文宽度折行。每次菜单最多派发一次动作；拒绝 shift、拖拽、取走虚拟图标。原生施法仍受装备、资源、冷却、目标与服务端事件约束。

验证：Java 285 项断言；独立 72 模组测试服 7 项真实协议验收，包括菜单点击后隐身效果、魔力下降、冷却出现，以及冷却禁用与虚拟物品保护。失败的前两次 QA 原始回执保留，第三次完整通过；未做真人手柄和第一人称截图验收。见 `docs/SKILL-SYSTEM-REVIEW.md`。

```powershell
$env:BOTGATE_LIBRARIES='D:/Projects/QiandengJi/server/mc/libraries'
python world/botgate-src/tests/run_tests.py
```
